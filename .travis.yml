language: go

go:
  - 1.9.x
  - 1.10.x
  - 1.11.x

before_install:
  - go get -u golang.org/x/lint/golint
  - go get github.com/axw/gocov/gocov

script:
  - test -z "`gofmt -l .`"
  - test -z "`golint ./...`"
  - go test -v -race ./...